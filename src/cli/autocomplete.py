ac_table = {
    'commands': [
        '--help',
        '--version',
        '--dataset',
        '--dataset-type',
        '--docarray-id',
        '--data-path',
        '--secret',
        '--data-url',
        '--quality',
    ]
    # Can also add completions list if we have more keyword to use with cli
    # e.g. jina-now start
}
