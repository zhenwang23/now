# should push this whole code to dockerhub to make sure, everyone can run this as one-liner
# alternative way of running is a bash script which clones this repo, creates an environment, installs requirements...